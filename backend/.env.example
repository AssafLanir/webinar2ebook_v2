# Backend Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# LLM API Keys (at least one required for AI features)
# =============================================================================

# OpenAI API Key (primary provider)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic API Key (fallback provider)
# Get yours at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# =============================================================================
# LLM Configuration (optional)
# =============================================================================

# Default provider: "openai" or "anthropic" (default: openai)
# LLM_DEFAULT_PROVIDER=openai

# Request timeout in seconds (default: 60)
# LLM_TIMEOUT_SECONDS=60

# Max retries per provider (default: 2)
# LLM_MAX_RETRIES=2

# =============================================================================
# MongoDB (optional - defaults shown)
# =============================================================================

# MongoDB connection string
# MONGODB_URI=mongodb://localhost:27017

# Database name
# MONGODB_DB_NAME=webinar2ebook
